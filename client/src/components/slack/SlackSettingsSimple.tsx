import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const SlackSettingsSimple = () => {
  return (
    <Card className="w-full">
      <CardHeader>
        <CardTitle>Slack Integration (Simple)</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a simple test component to check if the issue is resolved.</p>
      </CardContent>
    </Card>
  );
};

export default SlackSettingsSimple;